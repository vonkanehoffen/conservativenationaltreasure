http://conservativeNationalTreasure.com/ Site

Get started:

$ npm install
$ bower install

...to install the build tools & vendor libraries, then:

$ grunt dev

...to build the CSS and watch the LESS files for changes. 
Point a web server at index.html and you're done! 
